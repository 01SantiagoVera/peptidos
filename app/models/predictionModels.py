import pickle

def load_models():
    # with open('models/model_SVM_ABPs.pkl', 'rb') as file:
        #model_SVM = pickle.load(file)
    #with open('models/model_RF_ABPs.pkl', 'rb') as file:
       # model_RF = pickle.load(file)
    #with open('models/model_NN_ABPs.pkl', 'rb') as file:
       # model_NN = pickle.load(file)
    #return model_SVM, model_RF, model_NN
    return "modelo_simulado", "modelo_simulado", "modelo_simulado"

def make_prediction(models, sequence):
    # model_SVM, model_RF, model_NN = models
    # Aquí iría la lógica para procesar la secuencia y hacer la predicción
    prediction = "Predicción simulada"
    return prediction
